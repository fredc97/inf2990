import { ErrorInterface } from "./error-interface";
import { PopUpInterface } from "./pop-up-interface";

export interface MessageInterface {
    _error: ErrorInterface;
    _popUp: PopUpInterface;
}
